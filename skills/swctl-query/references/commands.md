# swctl Command Reference

Generated by `extract_swctl_commands.py` from `/usr/bin/swctl --help` at `2026-06-25T17:00:05+00:00`.

Use this as the authoritative local command and option reference for this workstation. Prefer `--display json` when command output needs parsing.

## Top-Level Commands

`browser`, `endpoint`, `instance`, `service`, `metrics`, `trace`, `trace-v2`, `health`, `dashboard`, `install`, `event`, `logs`, `completion`, `dependency`, `alarm`, `layer`, `process`, `profiling`, `records`, `menu`, `hierarchy`, `admin`

## Global Options

- `--config path`: file path of the default configurations. (default: "~/.skywalking.yml")
- `--base-url url`: base url of the OAP backend graphql service (default: "http://127.0.0.1:12800/graphql")
- `--admin-url url`: base url of the OAP admin-server REST service (default port 17128), used by `swctl admin ...` sub-commands. If empty, it is derived from `--base-url` by reusing its host with port 17128. [$SW_ADMIN_URL]
- `--grpc-addr <host:port>`: backend gRPC service address <host:port> (default: "127.0.0.1:11800")
- `--username username`: username of basic authorization
- `--password password`: password of basic authorization
- `--authorization authorization`: authorization header, can be something like `Basic base64(username:password)` or `Bearer jwt-token`, if `authorization` is set, `--username` and `--password` are ignored
- `--insecure value`: skip TLS certificate verification
- `--timezone timezone`: specifies the timezone where `--start` and `--end` are based, in the form of `+0800`. If `--timezone` is given in the command line option, then it's used directly. If the backend support the timezone API (since 6.5.0), CLI will try to get the timezone from backend, and use it. Otherwise, the CLI will use the current timezone in the current machine.
- `--debug`: enable debug mode, will print more detailed information at runtime (default: false)
- `--display style`: display style of the result, supported styles are: `json`, `yaml`, `table`, `graph`. (Not all display styles are supported in all commands due to data formats incompatibilities and the limits of Ascii Graph, like coloring in terminal, so in that cases please use `json`  or `yaml` instead.)
- `--help, -h`: show help (default: false)
- `--version, -v`: print the version (default: false)

## Command Details

## `browser`
- Aliases: `b`
- Description: Browser related sub-command
- Usage: `swctl browser command [command options] [arguments...]`
- Subcommands: `service`, `version`, `page`, `logs`

Options:
- `--help, -h`: show help (default: false)

### `browser service`
- Aliases: `s`, `svc`
- Description: Browser service related sub-command
- Usage: `swctl browser service command [command options] [arguments...]`
- Subcommands: `list`

Options:
- `--help, -h`: show help (default: false)

#### `browser service list`
- Aliases: `ls`
- Description: list the monitored browser services
- Usage: `This command lists all browser services if not "<service name>" is given, otherwise, it only lists the browser services matching the given "<service name>". Examples: 1. List all the browser services: $ swctl browser svc ls 2. List a specific browser service named "test-ui": $ swctl browser svc ls test-ui`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `browser version`
- Aliases: `v`, `instance`
- Description: Browser service version related sub-command
- Usage: `swctl browser version command [command options] [arguments...]`
- Subcommands: `list`

Options:
- `--help, -h`: show help (default: false)

#### `browser version list`
- Aliases: `ls`
- Description: list all monitored browser version of the given "--service-id" or "--service-name"
- Usage: `This command lists all version of the browser service, via service id or service name. Examples: 1. List all version of the browser service by service name "provider": $ swctl browser version ls --service-name test-ui 2. List all version of the browser service by service id "dGVzdC11aQ==.1"": $ swctl browser version ls --service-id dGVzdC11aQ==.1`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `browser page`
- Aliases: `p`, `endpoint`
- Description: Browser page related sub-command
- Usage: `swctl browser page command [command options] [arguments...]`
- Subcommands: `list`

Options:
- `--help, -h`: show help (default: false)

#### `browser page list`
- Aliases: `ls`
- Description: list all monitored browser page of the give "--service-id" or "--service-name"
- Usage: `This command lists all page of the browser service, via service id or service name. Examples: 1. List all page of the browser service by service name "test-ui": $ swctl browser page ls --service-name test-ui 2. List all page of the browser service by service id "dGVzdC11aQ==.1": $ swctl browser page ls --service-id dGVzdC11aQ==.1`

Options:
- `--limit <limit>`: returns at most <limit> endpoints (default: 100)
- `--help, -h`: show help (default: false)

### `browser logs`
- Aliases: `l`
- Description: Browser error log related sub-command
- Usage: `swctl browser logs command [command options] [arguments...]`
- Subcommands: `list`

Options:
- `--help, -h`: show help (default: false)

#### `browser logs list`
- Aliases: `ls`
- Description: List browser error logs according to the specified options
- Usage: `List browser error logs according to the specified options. Examples: 1. Query all logs: $ swctl browser logs list`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `endpoint`
- Aliases: `e`
- Description: Endpoint related sub-command
- Usage: `swctl endpoint command [command options] [arguments...]`
- Subcommands: `list`, `get`

Options:
- `--help, -h`: show help (default: false)

### `endpoint list`
- Aliases: `ls`
- Description: list all monitored endpoints of the given "--service-id" or "--service-name"
- Usage: `This command lists all endpoints of the service, via service id or service name. Examples: 1. List all endpoints of the service by service name "business-zone::projectC": $ swctl endpoint ls --service-name business-zone::projectC 2. List all endpoints of the service by service id "YnVzaW5lc3Mtem9uZTo6cHJvamVjdEM=.1": $ swctl endpoint ls --service-id YnVzaW5lc3Mtem9uZTo6cHJvamVjdEM=.1 3. Search endpoints like "projectC" of the service "business-zone::projectC": $ swctl endpoint ls --service-name business-zone::projectC --keyword projectC`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `endpoint get`
- Description: get monitored endpoint of the given <endpoint-id>
- Usage: `This command get single endpoint, via endpoint-id. Examples: 1. get single endpoint by endpoint id "cHJvdmlkZXI=.1_L3VzZXJz": $ swctl endpoint get cHJvdmlkZXI=.1_L3VzZXJz`

Options:
- `--help, -h`: show help (default: false)

## `instance`
- Aliases: `i`
- Description: Instance related sub-command
- Usage: `swctl instance command [command options] [arguments...]`
- Subcommands: `list`, `search`, `get`

Options:
- `--help, -h`: show help (default: false)

### `instance list`
- Aliases: `ls`
- Description: list all monitored instances of the given "--service-id" or "--service-name"
- Usage: `This command lists all instances of the service, via service id or service name. Examples: 1. List all instances of the service by service name "provider": $ swctl instance ls --service-name business-zone::projectC 2. List all instances of the service by service id "YnVzaW5lc3Mtem9uZTo6cHJvamVjdEM=.1": $ swctl instance ls --service-id YnVzaW5lc3Mtem9uZTo6cHJvamVjdEM=.1`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `instance search`
- Description: Filter the instance from the existing service instance list
- Usage: `swctl instance search [command options]`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `instance get`
- Description: get monitored instance of the given <instance-id>
- Usage: `This command get single instance, via instance-id. Examples: 1. get single instance by instance id "cHJvdmlkZXI=.1_cHJvdmlkZXIx": $ swctl instance get cHJvdmlkZXI=.1_cHJvdmlkZXIx`

Options:
- `--help, -h`: show help (default: false)

## `service`
- Aliases: `s`, `svc`
- Description: Service related sub-command
- Usage: `swctl service command [command options] [arguments...]`
- Subcommands: `list`, `layer`

Options:
- `--help, -h`: show help (default: false)

### `service list`
- Aliases: `ls`
- Description: list the monitored services
- Usage: `This command lists all services if no "<service name>" is given, otherwise, it only lists the services matching the given "<service name>". Examples: 1. List all the services: $ swctl svc ls 2. List a specific service named "projectC": $ swctl svc ls projectC`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `service layer`
- Aliases: `ly`
- Description: list the service list according to layer
- Usage: `This command lists the services matching the given "<layer name>". Examples: 2. List services in "GENERAL" layer: $ swctl svc ly GENERAL`

Options:
- `--help, -h`: show help (default: false)

## `metrics`
- Description: Query metrics defined in backend OAL
- Usage: `swctl metrics command [command options] [arguments...]`
- Subcommands: `single`, `nullable`, `linear`, `multiple-linear`, `thermodynamic`, `top`, `sorted`, `sampled-record`, `list`, `execute`

Options:
- `--help, -h`: show help (default: false)

### `metrics single`
- Description: query single-value metrics defined in backend OAL
- Usage: `Query single-value metrics defined in backend OAL. Examples: 1. Query the traffic load (calls per minute) of service "business-zone:projectC" $ swctl metrics single --name service_cpm --service-name business-zone::projectC 2. Query the traffic load (calls per minute) of service "business-zone:projectC" endpoint "/projectC/{value}" $ swctl metrics single --name endpoint_cpm --service-name business-zone::projectC --endpoint-name /projectC/{value}`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `metrics nullable`
- Description: query nullable-value metrics defined in backend OAL
- Usage: `Query nullable-value metrics defined in backend OAL. Examples: 1. Query the traffic load (calls per minute) of service "business-zone:projectC" $ swctl metrics nullable --name service_cpm --service-name business-zone::projectC 2. Query the traffic load (calls per minute) of service "business-zone:projectC" endpoint "/projectC/{value}" $ swctl metrics nullable --name endpoint_cpm --service-name business-zone::projectC --endpoint-name /projectC/{value}`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `metrics linear`
- Description: Query linear-type metrics defined in backend OAL
- Usage: `Query linear-type metrics defined in backend OAL Examples: 1. Query the response time of service "business-zone::projectB" $ swctl metrics linear --name=service_resp_time --service-name business-zone::projectB 2. Query the response time of service instance $ swctl metrics linear --name=service_instance_resp_time --service-name business-zone::projectB \ --instance-name d708c6bfea9f4d50902d1743302a6f50@10.170.0.12 3. Query the traffic (calls per minutes) between service "consumer" to service "provider" $ swctl metrics linear --name=service_relation_client_cpm --service-name consumer --dest-service-name provider`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `metrics multiple-linear`
- Description: Query multiple linear-type metrics defined in backend OAL
- Usage: `Query multiple linear-type metrics defined in backend OAL. Examples: 1. Query the global percentiles: $ swctl metrics multiple-linear --name all_percentile 2. Relabel the labels for better readability: $ swctl metrics multiple-linear --name all_percentile --labels=0,1,2,3,4 --relabels=P50,P75,P90,P95,P99`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `metrics thermodynamic`
- Aliases: `td`, `heatmap`, `hp`, `hm`
- Description: query thermodynamic-type metrics defined in backend OAL
- Usage: `Query the thermodynamic-type metrics defined in backend OAL. Examples: 1. Query the global heatmap: $ swctl metrics thermodynamic --scope all --name all_heatmap`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `metrics top`
- Description: query the top <n> entities sorted by the specified metrics
- Usage: `Query the top <n> entities sorted by the specified metrics. Examples: 1. Query the top 5 services whose sla are largest: $ swctl metrics top --name service_sla 5 2. Query the top 5 endpoints whose sla is largest: $ swctl metrics top --name endpoint_sla 5 3. Query the top 5 instances of service "boutique::adservice" whose sla are largest: $ swctl metrics top --name service_instance_sla --service-name boutique::adservice 5`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `metrics sorted`
- Description: query the top <n> entities sorted by the specified metrics
- Usage: `Query the top <n> entities sorted by the specified metrics. Examples: 1. Query the top 5 services whose sla are largest: $ swctl metrics sorted --name service_sla 5 2. Query the top 5 endpoints whose sla is largest: $ swctl metrics sorted --name endpoint_sla 5 3. Query the top 5 instances of service "boutique::adservice" whose sla are largest: $ swctl metrics sorted --name service_instance_sla --service-name boutique::adservice 5`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `metrics sampled-record`
- Description: query the top <n> entities sorted by the specified records
- Usage: `Query the top <n> entities sorted by the specified records. Examples: 1. Query the top 5 database statements whose execute duration are largest: $ swctl metrics sampled-record --name top_n_database_statement 5`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `metrics list`
- Aliases: `ls`
- Description: List metrics that could be queried
- Usage: `With this command, you can list all available metrics as well as their types and catalogs that can be used in other metrics command to get the details. Examples: 1. Query the metrics names start with "service_" $ swctl metrics list --regex "service_.*"`

Options:
- `--regex value`: filter metrics with regex
- `--help, -h`: show help (default: false)

### `metrics execute`
- Aliases: `exec`
- Description: Execute a metrics expression.
- Usage: `Execute a metrics expression. Examples: 1. Execute the given expression of service "business-zone::projectB" $ swctl metrics execute --expression="service_resp_time" --service-name business-zone::projectB`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `trace`
- Aliases: `t`
- Description: Trace related sub-command
- Usage: `swctl trace command [command options] <trace id>`
- Subcommands: `list`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `trace list`
- Aliases: `ls`
- Description: Query the monitored traces
- Usage: `Query the monitored traces. Examples: 1. Query all monitored traces: $ swctl trace ls 2. Query all monitored traces of service "business-zone::projectB": $ swctl trace ls --service-name "business-zone::projectB" 3. Query all monitored traces of endpoint "/projectB/{value}" of service "business-zone::projectB": $ swctl trace ls --service-name "business-zone::projectB" --endpoint-name "/projectB/{value}" 3. Query the monitored trace of id "321661b1-9a31-4e12-ad64-c8f6711f108d": $ swctl trace ls --trace-id "321661b1-9a31-4e12-ad64-c8f6711f108d" 4. Query the monitored trace of id "321661b1-9a31-4e12-ad64-c8f6711f108d" from cold-stage storage: $ swctl trace ls --trace-id "321661b1-9a31-4e12-ad64-c8f6711f108d" --cold`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `trace-v2`
- Aliases: `tv2`
- Description: Trace v2 related sub-command
- Usage: `swctl trace-v2 command [command options] [arguments...]`
- Subcommands: `list`

Options:
- `--help, -h`: show help (default: false)

### `trace-v2 list`
- Aliases: `ls`
- Description: Query the monitored traces
- Usage: `Query the monitored traces. Examples: 1. Query all monitored traces: $ swctl trace-v2 ls 2. Query all monitored traces of service "business-zone::projectB": $ swctl trace-v2 ls --service-name "business-zone::projectB" 3. Query all monitored traces of endpoint "/projectB/{value}" of service "business-zone::projectB": $ swctl trace-v2 ls --service-name "business-zone::projectB" --endpoint-name "/projectB/{value}" 3. Query the monitored trace of id "321661b1-9a31-4e12-ad64-c8f6711f108d": $ swctl trace-v2 ls --trace-id "321661b1-9a31-4e12-ad64-c8f6711f108d" 4. Query the monitored trace of id "321661b1-9a31-4e12-ad64-c8f6711f108d" from cold-stage storage: $ swctl trace-v2 ls --trace-id "321661b1-9a31-4e12-ad64-c8f6711f108d" --cold`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `health`
- Description: Checks whether OAP server is healthy
- Usage: `Checks whether OAP server is healthy. Before using this, please make sure the OAP enables the health checker, refer to https://skywalking.apache.org/docs/main/latest/en/setup/backend/backend-health-check/ Note: once enable gRPC TLS, checkHealth command would ignore server's cert. Examples: 1. Check health status from GraphQL and the gRPC endpoint listening on 10.0.0.1:8843 $ swctl health --grpc-addr=10.0.0.1:8843 2. Once the gRPC endpoint of OAP encrypts communication by TLS: $ swctl health --grpcTLS=true 3. Check health status from GraphQL service only: $ swctl health --grpc=false`

Options:
- `--grpc`: check OAP gRPC by HealthCheck service (default: true)
- `--grpcTLS`: use TLS for gRPC (default: false)
- `--help, -h`: show help (default: false)

## `dashboard`
- Aliases: `db`, `dash`
- Description: Dashboard related sub-command
- Usage: `swctl dashboard command [command options] [arguments...]`
- Subcommands: `global`, `global-metrics`

Options:
- `--help, -h`: show help (default: false)

### `dashboard global`
- Aliases: `g`
- Description: Display global dashboard
- Usage: `Display global dashboard Examples: 1. Display the global dashboard $ swctl dashboard global 2. Display the global dashboard with a customized template $ swctl dashboard global --template my-global-template.yml`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `dashboard global-metrics`
- Description: Query global metrics
- Usage: `swctl dashboard global-metrics [command options]`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `install`
- Aliases: `i`
- Description: Generates install manifest and applies it to a cluster
- Usage: `swctl install command [command options] [arguments...]`
- Subcommands: `manifest`

Options:
- `--help, -h`: show help (default: false)

### `install manifest`
- Aliases: `mf`
- Description: Output the Kubernetes manifest for installing OAP server and UI to stdout
- Usage: `swctl install manifest command [command options] [arguments...]`
- Subcommands: `oap`, `ui`

Options:
- `--help, -h`: show help (default: false)

#### `install manifest oap`
- Aliases: `o`
- Description: Output the Kubernetes manifest for installing OAP server to stdout
- Usage: `Examples: A customized resource file (oap-cr.yaml) to enable ALS analyzer and connect to elasticsearch cluster 'es1.foo:9200': spec: config: - name: SW_ENVOY_METRIC_ALS_HTTP_ANALYSIS value: k8s-mesh - name: SW_STORAGE value: elasticsearch - name: SW_STORAGE_ES_CLUSTER_NODES value: es1.foo:9200 1. Output manifest with default custom resource $ swctl install manifest oap 2. Load overlay custom resource from flag $ swctl install manifest oap -f oap-cr.yaml 3. Load overlay custom resource from stdin $ cat oap-cr.yaml | swctl install manifest oap -f=- 4. Apply directly to Kubernetes $ swctl install manifest oap -f oap-cr.yaml | kubectl apply -f-`

Options:
- `--name value`: The name of prefix of generated resources (default: "skywalking")
- `--namespace value`: The namespace where resource will be deployed (default: "skywalking-system") -f value             The custom resource file describing custom resources defined by swck
- `--help, -h`: show help (default: false)

#### `install manifest ui`
- Aliases: `u`
- Description: Output the Kubernetes manifest for installing UI to stdout
- Usage: `Examples: Some examples of customized resource overlay files (ui-cr.yaml) 1. Set OAP server address to 'oap.test'', use an ingress to expose UI spec: OAPServerAddress: oap.test service: ingress: host: ui.skywalking.test 2. Use a LoadBalancer to expose UI spec: service: serviceSpec: type: LoadBalancer ports: - name: page port: 80 targetPort: 8080 1. Output manifest with default custom resource $ swctl install manifest ui 2. Load overlay custom resource from flag $ swctl install manifest ui -f ui-cr.yaml 3. Load overlay custom resource from stdin $ cat ui-cr.yaml | swctl install manifest ui -f=- 4. Apply directly to Kubernetes $ swctl install manifest ui -f ui-cr.yaml | kubectl apply -f-`

Options:
- `--name value`: The name of prefix of generated resources (default: "skywalking")
- `--namespace value`: The namespace where resource will be deployed (default: "skywalking-system") -f value             The custom resource file describing custom resources defined by swck
- `--help, -h`: show help (default: false)

## `event`
- Description: Event related sub-command
- Usage: `swctl event command [command options] [arguments...]`
- Subcommands: `report`, `list`

Options:
- `--help, -h`: show help (default: false)

### `event report`
- Aliases: `r`
- Description: Report an event to OAP server via gRPC
- Usage: `swctl event report [command options] [parameters...] DESCRIPTION: When reporting an event, you typically execute the command twice, one for starting of the event (--start-time is required) and the other one for ending of the event (--uuid and --end-time are required).\n You can also report an event once with start time and end time.`

Options:
- `--uuid ID`: Unique ID of the event, if not specified, a random one will be generated. (default: "263a3eb8-a779-4399-9669-3ced1854f261")
- `--name value`: The name of the event. For example, 'Reboot' and 'Upgrade' etc.
- `--type value`: The type of the event. (default: Normal)
- `--message value`: The detail of the event. This should be a one-line message that briefly describes why the event is reported.
- `--start-time value`: The start time (in milliseconds) of the event, measured between the current time and midnight, January 1, 1970 UTC. (default: 0)
- `--end-time value`: The end time (in milliseconds) of the event, measured between the current time and midnight, January 1, 1970 UTC. (default: 0)
- `--layer value`: Name of the layer to which the event belongs (case-insensitive), which can be queried via 'swctl layer list'
- `--help, -h`: show help (default: false)

### `event list`
- Aliases: `ls`
- Description: List events
- Usage: `List events Examples: 1. List all events: $ swctl event list`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `logs`
- Aliases: `l`
- Description: Logs related sub-command
- Usage: `swctl logs command [command options] [arguments...]`
- Subcommands: `list`

Options:
- `--help, -h`: show help (default: false)

### `logs list`
- Aliases: `ls`
- Description: List logs according to the specified options
- Usage: `List logs according to the specified options. Examples: 1. Query all logs: $ swctl logs list 2. Query the logs related to trace id 3d56f33f-bcd3-4e40-9e4f-5dc547998ef5 $ swctl logs list --trace-id 3d56f33f-bcd3-4e40-9e4f-5dc547998ef5`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `completion`
- Description: Output shell completion code for bash, zsh, fish, and powershell
- Usage: `swctl completion command [command options] [arguments...]`
- Subcommands: `bash`, `fish`, `zsh`, `powershell`

Options:
- `--help, -h`: show help (default: false)

### `completion bash`
- Aliases: `b`
- Description: Output shell completion code for bash
- Usage: `swctl completion bash [command options] [parameters...]`

Options:
- `--help, -h`: show help (default: false)

### `completion fish`
- Aliases: `f`
- Description: Output shell completion code for fish
- Usage: `swctl completion fish [command options] [parameters...]`

Options:
- `--help, -h`: show help (default: false)

### `completion zsh`
- Aliases: `z`
- Description: Output shell completion code for zsh
- Usage: `swctl completion zsh [command options] [parameters...]`

Options:
- `--help, -h`: show help (default: false)

### `completion powershell`
- Aliases: `p`
- Description: Output shell completion code for powershell
- Usage: `swctl completion powershell [command options] [parameters...]`

Options:
- `--help, -h`: show help (default: false)

## `dependency`
- Aliases: `dep`
- Description: Dependency related subcommand
- Usage: `swctl dependency command [command options] [arguments...]`
- Subcommands: `endpoint`, `service`, `instance`, `process`, `global`

Options:
- `--help, -h`: show help (default: false)

### `dependency endpoint`
- Aliases: `ep`
- Description: Query the dependencies of the given endpoint
- Usage: `swctl dependency endpoint [command options]`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `dependency service`
- Aliases: `svc`
- Description: Query the dependencies of given service
- Usage: `swctl dependency service [command options]`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `dependency instance`
- Aliases: `instc`
- Description: Query the instance topology, based on the given source service and destination service
- Usage: `swctl dependency instance [command options]`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `dependency process`
- Aliases: `pros`
- Description: Query the process topology, based on the given service instance
- Usage: `swctl dependency process [command options]`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `dependency global`
- Aliases: `glb`
- Description: Query the global dependencies
- Usage: `swctl dependency global [command options]`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `alarm`
- Description: Alarm related sub-command
- Usage: `swctl alarm command [command options] [arguments...]`
- Subcommands: `list`, `autocomplete-keys`, `autocomplete-values`

Options:
- `--help, -h`: show help (default: false)

### `alarm list`
- Aliases: `ls`
- Description: List alarms
- Usage: `List alarms Examples: 1. List all alarms: $ swctl alarm list`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `alarm autocomplete-keys`
- Aliases: `ks`
- Description: Query autocomplete Keys
- Usage: `Query autocomplete keys Examples: 1. Query autocomplete keys: $ swctl alarm autocomplete-keys`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `alarm autocomplete-values`
- Aliases: `vs`
- Description: Query autocomplete Values
- Usage: `Query autocomplete values Examples: 1. Query autocomplete values: $ swctl alarm autocomplete-values --key=tagKey`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `layer`
- Description: Layer related sub-command
- Usage: `swctl layer command [command options] [arguments...]`
- Subcommands: `list`

Options:
- `--help, -h`: show help (default: false)

### `layer list`
- Aliases: `ls`
- Description: List layers
- Usage: `List layers Examples: 1. List all layers: $ swctl layer list`

Options:
- `--help, -h`: show help (default: false)

## `process`
- Aliases: `p`
- Description: Process related sub-command
- Usage: `swctl process command [command options] [arguments...]`
- Subcommands: `list`, `get`, `estimate`

Options:
- `--help, -h`: show help (default: false)

### `process list`
- Aliases: `ls`
- Description: list all monitored processes under the instance
- Usage: `This command lists all processes of the service-instance. Examples: 3. List all processes by instance name "provider-01" and service name "provider": $ swctl process ls --instance-name provider-01 --service-name provider 4. List all processes by instance id "cHJvdmlkZXI=.1_cHJvdmlkZXIx": $ swctl process ls --instance-id cHJvdmlkZXI=.1_cHJvdmlkZXIx`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `process get`
- Description: get monitored process of the given <process-id>
- Usage: `This command get single process, via process-id. Examples: 1. get single process by process id "2b9e46c13c91803695a4364257415e523af7cbf17bf4058e025c16b944a6a85b": $ swctl process get 2b9e46c13c91803695a4364257415e523af7cbf17bf4058e025c16b944a6a85b`

Options:
- `--help, -h`: show help (default: false)

### `process estimate`
- Description: Process estimate related sub-command
- Usage: `swctl process estimate command [command options] [arguments...]`
- Subcommands: `scale`

Options:
- `--help, -h`: show help (default: false)

#### `process estimate scale`
- Description: estimate monitored process scale of the given id or name in service and labels
- Usage: `This command estimate monitored process scale, via id or name in service and labels. Examples: 1. Estimate process scale by service name "abc" with labels "t1,t2": $ swctl process estimate scale --service-name abc --labels t1,t2`

Options:
- `--labels labels`: the labels by which labels of the process, multiple labels split by ',': l1,l2
- `--help, -h`: show help (default: false)

## `profiling`
- Description: profiling related sub-command
- Usage: `swctl profiling command [command options] [arguments...]`
- Subcommands: `trace`, `ebpf`, `continuous`, `async`, `pprof`

Options:
- `--help, -h`: show help (default: false)

### `profiling trace`
- Description: trace profiling related sub-command
- Usage: `swctl profiling trace command [command options] [arguments...]`
- Subcommands: `create`, `list`, `logs`, `segment-list`, `analysis`

Options:
- `--help, -h`: show help (default: false)

#### `profiling trace create`
- Aliases: `c`
- Description: Create a new trace profiling task
- Usage: `Create a new trace profiling task Examples: 1. Create trace profiling task $ swctl profiling trace create --service-name=service-name --endpoint=endpoint --start-time=1627656127860 --duration=5 \ --min-duration-threshold=0 --dump-period=10 --max-sampling-count=9`

Options:
- `--start-time value`: profile task start time(millisecond). (default: 0)
- `--duration value`: profile task continuous time(minute). (default: 0)
- `--min-duration-threshold value`: profiled endpoint must greater duration(millisecond). (default: 0)
- `--dump-period value`: profiled endpoint dump period(millisecond). (default: 0)
- `--max-sampling-count value`: profile task max sampling count. (default: 0)
- `--help, -h`: show help (default: false)

#### `profiling trace list`
- Aliases: `l`
- Description: Query trace profiling task list
- Usage: `Query trace profiling task list Examples: 1. Query all trace profiling tasks $ swctl profiling trace list --service-name=service-name --endpoint-name=endpoint 2. Query trace profiling tasks of service "business-zone::projectC", endpoint "/projectC/{value}" $ swctl profiling trace list --service-name=business-zone::projectC --endpoint-name=/projectC/{value}`

Options:
- `--help, -h`: show help (default: false)

#### `profiling trace logs`
- Aliases: `logs`
- Description: Query trace profiling task log list
- Usage: `Query trace profiling task log list Examples: 1. Query all trace profiling logs of task id "task-id" $ swctl profiling trace logs --task-id=task-id`

Options:
- `--task-id value`: profile task id.
- `--help, -h`: show help (default: false)

#### `profiling trace segment-list`
- Aliases: `sl`
- Description: Query profiling trace task segments list
- Usage: `Query profiling trace task segments list Examples: 1. Query profiled segments list $ swctl profiling trace segment-list --service-name=service-name --endpoint-name=endpoint`

Options:
- `--help, -h`: show help (default: false)

#### `profiling trace analysis`
- Aliases: `pa`
- Description: Analyze profiled segment.
- Usage: `swctl profiling trace analysis [command options] [parameters...]`

Options:
- `--segment-ids value`: profiled segment ids, multiple id split by ',': s1,s2
- `--time-ranges value`: need to analyze time ranges in the segment: start-end,start-end
- `--help, -h`: show help (default: false)

### `profiling ebpf`
- Description: eBPF Profiling related sub-command
- Usage: `swctl profiling ebpf command [command options] [arguments...]`
- Subcommands: `create`, `keep`, `list`, `schedules`, `analysis`

Options:
- `--help, -h`: show help (default: false)

#### `profiling ebpf create`
- Description: eBPF Profiling task create related sub-command
- Usage: `swctl profiling ebpf create command [command options] [arguments...]`
- Subcommands: `prepare`, `fixed`, `network`

Options:
- `--help, -h`: show help (default: false)

##### `profiling ebpf create prepare`
- Aliases: `pre`
- Description: Prepare to a new ebpf profiling task, typically used to query data before creating a task
- Usage: `Prepare to a new ebpf profiling task, typically used to query data before creating a task Examples: 1. Prepare ebpf profiling fixed time task $ swctl profiling ebpf create prepare --service-id=abc`

Options:
- `--help, -h`: show help (default: false)

##### `profiling ebpf create fixed`
- Aliases: `ft`
- Description: Create a new ebpf profiling fixed time task
- Usage: `Create a new ebpf profiling fixed time task Examples: 1. Create ebpf profiling fixed time task $ swctl profiling ebpf fixed --service-id=abc --process-id=abc --duration=1m --target-type=ON_CPU`

Options:
- `--labels labels`: the labels by which labels of the process need to be profiling, multiple labels split by ',': l1,l2
- `--duration value`: profiling task continuous time.
- `--start-time value`: profiling task start time(millisecond). (default: 0)
- `--target-type target-type`: the target-type by the way of profiling the process (default: ON_CPU)
- `--help, -h`: show help (default: false)

##### `profiling ebpf create network`
- Aliases: `net`
- Description: Create a new ebpf network profiling task
- Usage: `Create a new ebpf network profiling task Examples: 1. Create ebpf network profiling task $ swctl profiling ebpf create network --service-instance-id=abc`

Options:
- `--sampling-config sampling-config`: the sampling-config file define how to sampling the network data, if not then then ignore data sampling
- `--help, -h`: show help (default: false)

#### `profiling ebpf keep`
- Description: eBPF Profiling task keep-alive related sub-command
- Usage: `swctl profiling ebpf keep command [command options] [arguments...]`
- Subcommands: `network`

Options:
- `--help, -h`: show help (default: false)

##### `profiling ebpf keep network`
- Aliases: `net`
- Description: Keep alive the exist ebpf network profiling task
- Usage: `Keep alive the exist ebpf network profiling task Examples: 1. Keep alive the ebpf network profiling task $ swctl profiling ebpf keep network --task-id=abc`

Options:
- `--task-id task-id`: the task-id of the network profiling task
- `--help, -h`: show help (default: false)

#### `profiling ebpf list`
- Aliases: `ls`
- Description: query ebpf profiling task list
- Usage: `This command lists all ebpf profiling task, via id or name in service, instance or process. Example： 1. Query profiling tasks of service "business-zone::projectC" $ swctl profiling ebpf list --service-name=service-name`

Options:
- `--trigger value`: the trigger type for the profiling task (default: FIXED_TIME)
- `--help, -h`: show help (default: false)

#### `profiling ebpf schedules`
- Aliases: `ss`
- Description: query ebpf profiling task schedules
- Usage: `This command lists all schedule of the ebpf profiling task, via id of task. Example： 1. Query profiling schedules of task id "abc" $ swctl profiling ebpf schedules --task-id=abc`

Options:
- `--task-id task-id`: the task-id by which task are scheduled
- `--help, -h`: show help (default: false)

#### `profiling ebpf analysis`
- Aliases: `as`
- Description: analyze ebpf profiling task
- Usage: `This command analysis profiling task, via id of task and time ranges. Example: 1. Analysis profiling tasks of task id "abc" and time range in 1648020042869 to 1648020100764. $ swctl profiling ebpf analysis --schedule-id=abc --time-ranges=1648020042869-1648020100764`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `profiling continuous`
- Description: Continuous Profiling related sub-command
- Usage: `swctl profiling continuous command [command options] [arguments...]`
- Subcommands: `set`, `list`, `monitoring`

Options:
- `--help, -h`: show help (default: false)

#### `profiling continuous set`
- Aliases: `s`
- Description: Set the continuous profiling policy to service
- Usage: `Set the continuous profiling policy to service Examples: 1. Set the service continuous profiling policy $ swctl profiling continuous set --service-name=abc --config=/path/to/config.yaml`

Options:
- `--config config`: the config file define the service policy configuration, if not provide then make the service policy is empty
- `--help, -h`: show help (default: false)

#### `profiling continuous list`
- Aliases: `ls`
- Description: query service continuous profiling policy
- Usage: `This command lists all service continuous profiling policy, via id or name in service. Example： 1. Query continuous profiling policy of service "business-zone::projectC" $ swctl profiling continuous list --service-name=business-zone::projectC`

Options:
- `--help, -h`: show help (default: false)

#### `profiling continuous monitoring`
- Aliases: `monitor`
- Description: query all continuous profiling monitoring instances through service and policy target
- Usage: `Query all continuous profiling monitoring instances through service and policy target. Example: 1. Query all continuous profiling monitoring instances through service "business-zone::projectC" and policy target "ON_CPU" $ swctl profiling continuous monitoring --service-name=business-zone::projectC --target=ON_CPU`

Options:
- `--target value`: policy target type
- `--help, -h`: show help (default: false)

### `profiling async`
- Description: async profiler related sub-command
- Usage: `swctl profiling async command [command options] [arguments...]`
- Subcommands: `create`, `list`, `progress`, `analysis`

Options:
- `--help, -h`: show help (default: false)

#### `profiling async create`
- Aliases: `c`
- Description: Create a new async profiler task
- Usage: `Create a new async profiler task Examples: 1. Create async-profiler task $ swctl profiling async create --service-name=service-name --duration=60 --events=cpu,alloc \ --instance-name-list=instance-name1,instance-name2 --exec-args=interval=50ms`

Options:
- `--duration value`: task continuous time(second). (default: 0)
- `--events value`: which event types this task needs to collect.
- `--exec-args value`: other async-profiler execution options, e.g. alloc=2k,lock=2s.
- `--help, -h`: show help (default: false)

#### `profiling async list`
- Aliases: `l`
- Description: Query async-profiler task list
- Usage: `Query async-profiler task list Examples: 1. Query all async-profiler tasks $ swctl profiling async list --service-name=service-name`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

#### `profiling async progress`
- Aliases: `p`
- Description: Query async-profiler task progress
- Usage: `Query async-profiler task progress Examples: 1. Query task progress, including task logs and successInstances and errorInstances $ swctl profiling async progress --task-id=task-id`

Options:
- `--task-id value`: async profiler task id.
- `--help, -h`: show help (default: false)

#### `profiling async analysis`
- Aliases: `a`
- Description: Query async-profiler analysis
- Usage: `Query async-profiler analysis Examples: 1. Query the flame graph produced by async-profiler $ swctl profiling async analysis --service-name=service-name --task-id=task-id \ --instance-name-list=instance-name1,instance-name2 --event=execution_sample`

Options:
- `--task-id value`: async-profiler task id
- `--event value`: which event types this task needs to collect.
- `--help, -h`: show help (default: false)

### `profiling pprof`
- Description: pprof related sub-command
- Usage: `swctl profiling pprof command [command options] [arguments...]`
- Subcommands: `create`, `list`, `progress`, `analysis`

Options:
- `--help, -h`: show help (default: false)

#### `profiling pprof create`
- Aliases: `c`
- Description: Create a new pprof task
- Usage: `Create a new pprof task Examples: 1. Create pprof task $ swctl profiling pprof create --service-name=service-name --duration=10 --events=mutex \ --instance-name-list=instance-name1,instance-name2 --dump-period=1`

Options:
- `--duration value`: task continuous time(minute), required for cpu, block and mutex events.  (default: 0)
- `--events value`: which event types this task needs to collect.
- `--dump-period value`: pprof dump period parameters, required for block and mutex events.
- `--help, -h`: show help (default: false)

#### `profiling pprof list`
- Aliases: `l`
- Description: Query pprof task list
- Usage: `Query pprof task list Examples: 1. Query all pprof tasks $ swctl profiling pprof list --service-name=service-name`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

#### `profiling pprof progress`
- Aliases: `p`
- Description: Query pprof task progress
- Usage: `Query pprof task progress Examples: 1. Query task progress, including task logs and successInstances and errorInstances $ swctl profiling pprof progress --task-id=task-id`

Options:
- `--task-id value`: pprof task id.
- `--help, -h`: show help (default: false)

#### `profiling pprof analysis`
- Aliases: `a`
- Description: Query pprof analysis
- Usage: `Query pprof analysis Examples: 1. Query the flame graph produced by pprof $ swctl profiling pprof analysis --service-name=service-name --task-id=task-id \ --instance-name-list=instance-name1,instance-name2`

Options:
- `--task-id value`: pprof task id
- `--help, -h`: show help (default: false)

## `records`
- Aliases: `rec`
- Description: Records related sub-command
- Usage: `swctl records command [command options] [arguments...]`
- Subcommands: `list`

Options:
- `--help, -h`: show help (default: false)

### `records list`
- Aliases: `ls`
- Description: List records according to the specified options
- Usage: `List the top <n> records according to the specified options. Examples: 1. Query the top 5 database statements whose execute duration are largest: $ swctl records list --name top_n_database_statement 5`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

## `menu`
- Description: Management the UI menu
- Usage: `swctl menu command [command options] [arguments...]`
- Subcommands: `get`

Options:
- `--help, -h`: show help (default: false)

### `menu get`
- Description: Get the UI menu items
- Usage: `Get the UI menu items. Examples: 1. Get the UI menu items: $swctl menu get`

Options:
- `--help, -h`: show help (default: false)

## `hierarchy`
- Description: Hierarchy related sub-command
- Usage: `swctl hierarchy command [command options] [arguments...]`
- Subcommands: `service`, `instance`, `layer-levels`

Options:
- `--help, -h`: show help (default: false)

### `hierarchy service`
- Aliases: `svc`
- Description: Query the hierarchy of given service
- Usage: `swctl hierarchy service [command options]`

Options:
- `--layer value`: Name of the layer to query the hierarchy of this layer
- `--help, -h`: show help (default: false)

### `hierarchy instance`
- Aliases: `inst`
- Description: Query the hierarchy of given service instance
- Usage: `swctl hierarchy instance [command options]`

Options:
- `--layer value`: Name of the layer to query the hierarchy of this layer
- `--help, -h`: show help (default: false)

### `hierarchy layer-levels`
- Aliases: `ll`
- Description: Query all layer hierarchy levels
- Usage: `swctl hierarchy layer-levels [command options]`

Options:
- `--help, -h`: show help (default: false)

## `admin`
- Description: Admin (REST) sub-commands that talk to the OAP admin-server (default port 17128)
- Usage: `swctl admin command [command options] [arguments...]`
- Subcommands: `preflight`, `cluster`, `config`, `alarm`, `inspect`, `ui-template`, `runtime-rule`, `dsl-debug`, `oal`

Options:
- `--help, -h`: show help (default: false)

### `admin preflight`
- Description: Detect which admin feature modules are enabled on the OAP admin-server
- Usage: `Reads the effective configuration from the admin host and reports which feature modules (status, inspect, ui-management, dsl-debugging, runtime-rule) are enabled. Examples: 1. Check admin feature availability: $ swctl admin preflight`

Options:
- `--help, -h`: show help (default: false)

### `admin cluster`
- Description: Inspect the OAP cluster from the admin-server `status` module
- Usage: `swctl admin cluster command [command options] [arguments...]`
- Subcommands: `nodes`

Options:
- `--help, -h`: show help (default: false)

#### `admin cluster nodes`
- Description: List the OAP cluster peer nodes (GET /status/cluster/nodes)
- Usage: `List the OAP cluster peer nodes as seen by the cluster coordinator. Examples: 1. List cluster nodes: $ swctl admin cluster nodes`

Options:
- `--help, -h`: show help (default: false)

### `admin config`
- Description: Inspect the OAP effective configuration and TTL from the admin-server
- Usage: `swctl admin config command [command options] [arguments...]`
- Subcommands: `dump`, `ttl`

Options:
- `--help, -h`: show help (default: false)

#### `admin config dump`
- Description: Dump the OAP node's effective, secrets-redacted configuration (GET /debugging/config/dump)
- Usage: `Dump the effective configuration of the OAP node as a flat map of "<module>.<provider>.<property>" keys. Secrets are redacted by OAP. Examples: 1. Dump the effective configuration: $ swctl admin config dump`

Options:
- `--help, -h`: show help (default: false)

#### `admin config ttl`
- Description: Show the effective TTL configuration (GET /status/config/ttl)
- Usage: `Show the effective metric / record / trace / log TTL bounds. Examples: 1. Show the effective TTL configuration: $ swctl admin config ttl`

Options:
- `--help, -h`: show help (default: false)

### `admin alarm`
- Description: Inspect alarm runtime status from the admin-server `status` module
- Usage: `swctl admin alarm command [command options] [arguments...]`
- Subcommands: `rules`, `rule`

Options:
- `--help, -h`: show help (default: false)

#### `admin alarm rules`
- Description: List the loaded alarm rules per OAP node (GET /status/alarm/rules)
- Usage: `List the loaded alarm rules, fanned out across every OAP node. Examples: 1. List alarm rules: $ swctl admin alarm rules`

Options:
- `--help, -h`: show help (default: false)

#### `admin alarm rule`
- Description: Show one alarm rule's definition and running state (GET /status/alarm/{ruleId}[/{entityName}])
- Usage: `Show the definition and running state of a single alarm rule. When an entity name is given, the per-entity evaluation/window state is returned instead. Examples: 1. Show a rule's running state: $ swctl admin alarm rule service_resp_time_rule 2. Show the per-entity state of a rule: $ swctl admin alarm rule service_resp_time_rule mock_b_service`

Options:
- `--help, -h`: show help (default: false)

### `admin inspect`
- Description: Browse the metric catalog and entities from the admin-server `inspect` module
- Usage: `swctl admin inspect command [command options] [arguments...]`
- Subcommands: `metrics`, `entities`, `values`

Options:
- `--help, -h`: show help (default: false)

#### `admin inspect metrics`
- Description: List the registered metric catalog (GET /inspect/metrics)
- Usage: `List the registered metrics with their type, scope and supported downsamplings. Examples: 1. List every metric: $ swctl admin inspect metrics 2. List service metrics that /inspect/entities accepts: $ swctl admin inspect metrics --catalog SERVICE --mqe-queryable`

Options:
- `--regex regex`: filter metric names by a Java regex (default matches all)
- `--type type`: filter by metric type (REGULAR_VALUE / LABELED_VALUE / HEATMAP / SAMPLED_RECORD); repeatable
- `--catalog catalog`: filter by catalog (SERVICE / SERVICE_INSTANCE / ENDPOINT / *_RELATION); repeatable
- `--mqe-queryable`: return only metrics that /inspect/entities accepts (REGULAR_VALUE, LABELED_VALUE) (default: false)
- `--help, -h`: show help (default: false)

#### `admin inspect entities`
- Description: Enumerate the entities holding values for a metric (GET /inspect/entities)
- Usage: `Enumerate the entities currently holding values for a metric over a time range. Each row carries an MQE-ready entity to paste into a follow-up "swctl metrics exec" (execExpression) query. Only REGULAR_VALUE / LABELED_VALUE metrics are accepted. Examples: 1. Entities reporting service_cpm in the last 30 minutes: $ swctl admin inspect entities --metric service_cpm 2. Inspect a metric persisted by ANOTHER OAP (not defined on this OAP) — supply its value column + type so the OAP resolves it from storage without its local registry: $ swctl admin inspect entities --metric meter_foo --value-column value --value-type LONG`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

#### `admin inspect values`
- Description: Read the VALUES of metric(s) persisted by another OAP (POST /inspect/values)
- Usage: `Evaluate an MQE expression over metric(s) the target OAP does not define locally, by supplying each foreign metric's metadata. Returns the native MQE result (the same shape the UI renders for catalog metrics). The entity scope is inferred from which name flags are set. Example — read a foreign service metric's value series: $ swctl admin inspect values --expression meter_foo --service-name my-svc \ --foreign-metric meter_foo,value,LONG`

Options:
- `--start value`: "start" and "end" specify a time range during which the query is preformed, they can be absolute time like "2019-01-01 12", "2019-01-01 1213", or relative time (to the

### `admin ui-template`
- Description: Manage dashboard templates via the admin-server `ui-management` module
- Usage: `swctl admin ui-template command [command options] [arguments...]`
- Subcommands: `list`, `get`, `create`, `update`, `disable`

Options:
- `--help, -h`: show help (default: false)

#### `admin ui-template list`
- Description: List all dashboard templates (GET /ui-management/templates)
- Usage: `List all dashboard templates. Examples: 1. List enabled templates: $ swctl admin ui-template list 2. Include soft-disabled templates: $ swctl admin ui-template list --include-disabled`

Options:
- `--include-disabled`: also return soft-disabled templates (default: false)
- `--help, -h`: show help (default: false)

#### `admin ui-template get`
- Description: Get a single dashboard template by ID (GET /ui-management/templates/{id})
- Usage: `swctl admin ui-template get [command options] <id>`

Options:
- `--help, -h`: show help (default: false)

#### `admin ui-template create`
- Description: Add a new dashboard template (POST /ui-management/templates)
- Usage: `Add a new dashboard template. The file holds the JSON-encoded template configuration. An id is required by OAP; a random UUID is generated when --id is omitted. Examples: 1. Create a template from a file: $ swctl admin ui-template create -f my-dashboard.json`

Options:
- `--id id`: template id; a random UUID is generated when omitted
- `--help, -h`: show help (default: false)

#### `admin ui-template update`
- Description: Update an existing dashboard template (PUT /ui-management/templates)
- Usage: `Update an existing dashboard template by ID with a new configuration. Examples: 1. Update a template: $ swctl admin ui-template update --id <uuid> -f my-dashboard.json`

Options:
- `--id id`: id of the template to update --file path, -f path  path to the JSON-encoded template configuration
- `--help, -h`: show help (default: false)

#### `admin ui-template disable`
- Description: Soft-disable a dashboard template (POST /ui-management/templates/{id}/disable)
- Usage: `swctl admin ui-template disable [command options] <id>`

Options:
- `--help, -h`: show help (default: false)

### `admin runtime-rule`
- Description: Hot-update MAL / LAL rules via the admin-server `receiver-runtime-rule` module
- Usage: `swctl admin runtime-rule command [command options] [arguments...]`
- Subcommands: `list`, `bundled`, `get`, `add`, `inactivate`, `delete`, `dump`

Options:
- `--help, -h`: show help (default: false)

#### `admin runtime-rule list`
- Description: List the live rule state per node (GET /runtime/rule/list)
- Usage: `swctl admin runtime-rule list [command options]`

Options:
- `--catalog catalog`: rule catalog: otel-rules / log-mal-rules / telegraf-rules / lal
- `--help, -h`: show help (default: false)

#### `admin runtime-rule bundled`
- Description: List the static (bundled) rule twins for a catalog (GET /runtime/rule/bundled)
- Usage: `swctl admin runtime-rule bundled [command options]`

Options:
- `--catalog catalog`: rule catalog: otel-rules / log-mal-rules / telegraf-rules / lal
- `--with-content`: include the bundled rule content (default: true)
- `--help, -h`: show help (default: false)

#### `admin runtime-rule get`
- Description: Fetch a single rule's content + metadata (GET /runtime/rule)
- Usage: `swctl admin runtime-rule get [command options]`

Options:
- `--catalog catalog`: rule catalog: otel-rules / log-mal-rules / telegraf-rules / lal
- `--name name`: rule name
- `--source source`: source to read: runtime (default, DAO first) or bundled (static twin only)
- `--help, -h`: show help (default: false)

#### `admin runtime-rule add`
- Aliases: `add-or-update`
- Description: Push a new or updated rule from a YAML file (POST /runtime/rule/addOrUpdate)
- Usage: `Push a new or updated MAL / LAL rule. The file holds the raw rule YAML. Examples: 1. Apply a MAL rule: $ swctl admin runtime-rule add --catalog otel-rules --name vm -f vm.yaml`

Options:
- `--catalog catalog`: rule catalog: otel-rules / log-mal-rules / telegraf-rules / lal
- `--name name`: rule name --file path, -f path      path to the rule YAML
- `--allow-storage-change`: approve an edit that moves the rule's storage identity (structural change) (default: false)
- `--force`: re-run apply even when the content is byte-identical (default: false)
- `--help, -h`: show help (default: false)

#### `admin runtime-rule inactivate`
- Description: Turn a rule off (POST /runtime/rule/inactivate)
- Usage: `swctl admin runtime-rule inactivate [command options]`

Options:
- `--catalog catalog`: rule catalog: otel-rules / log-mal-rules / telegraf-rules / lal
- `--name name`: rule name
- `--help, -h`: show help (default: false)

#### `admin runtime-rule delete`
- Description: Remove a rule (POST /runtime/rule/delete)
- Usage: `Remove a rule. An active rule must be inactivated first (the server returns a 409 requires_inactivate_first otherwise). Examples: 1. Delete an inactivated rule: $ swctl admin runtime-rule delete --catalog otel-rules --name vm 2. Revert a rule to its bundled twin: $ swctl admin runtime-rule delete --catalog otel-rules --name vm --mode revertToBundled`

Options:
- `--catalog catalog`: rule catalog: otel-rules / log-mal-rules / telegraf-rules / lal
- `--name name`: rule name
- `--mode mode`: deletion mode; pass revertToBundled to restore the static twin
- `--help, -h`: show help (default: false)

#### `admin runtime-rule dump`
- Description: Download a tar.gz snapshot of all rules (GET /runtime/rule/dump)
- Usage: `Download a tar.gz snapshot of all rules, or one catalog's. Examples: 1. Dump every rule to a file: $ swctl admin runtime-rule dump -o rules.tar.gz 2. Dump one catalog: $ swctl admin runtime-rule dump --catalog otel-rules -o otel.tar.gz`

Options:
- `--catalog catalog`: rule catalog: otel-rules / log-mal-rules / telegraf-rules / lal --output path, -o path    path to write the tar.gz to (default: stdout)
- `--help, -h`: show help (default: false)

### `admin dsl-debug`
- Description: Live MAL / LAL / OAL debugger via the admin-server `dsl-debugging` module
- Usage: `swctl admin dsl-debug command [command options] [arguments...]`
- Subcommands: `status`, `sessions`, `session`

Options:
- `--help, -h`: show help (default: false)

#### `admin dsl-debug status`
- Description: Show the dsl-debugging module health snapshot (GET /dsl-debugging/status)
- Usage: `swctl admin dsl-debug status [command options]`

Options:
- `--help, -h`: show help (default: false)

#### `admin dsl-debug sessions`
- Description: List the active debug sessions (GET /dsl-debugging/sessions)
- Usage: `swctl admin dsl-debug sessions [command options]`

Options:
- `--help, -h`: show help (default: false)

#### `admin dsl-debug session`
- Description: Start / poll / stop a single debug session
- Usage: `swctl admin dsl-debug session command [command options] [arguments...]`
- Subcommands: `start`, `get`, `stop`

Options:
- `--help, -h`: show help (default: false)

##### `admin dsl-debug session start`
- Description: Start a debug capture session (POST /dsl-debugging/session)
- Usage: `Start a sample-based debug capture session. Examples: 1. Debug a MAL metric: $ swctl admin dsl-debug session start --catalog otel-rules --name vm --rule-name vm_memory_used`

Options:
- `--catalog catalog`: session catalog: otel-rules / log-mal-rules / telegraf-rules / lal / oal
- `--name name`: rule file name (MAL/LAL) or OAL source class name
- `--rule-name name`: the metric / rule name within the file (OAL: same as --name)
- `--client-id id`: stable per-debug-context id; a random UUID is generated when omitted
- `--granularity granularity`: LAL capture granularity: block (default) or statement
- `--record-cap value`: max records to capture before the session is full (1-100) (default: 0)
- `--retention-millis value`: session wall-clock retention in ms (max 3600000 = 1h) (default: 0)
- `--help, -h`: show help (default: false)

##### `admin dsl-debug session get`
- Description: Poll a session's captured records (GET /dsl-debugging/session/{id})
- Usage: `swctl admin dsl-debug session get [command options] <sessionId>`

Options:
- `--help, -h`: show help (default: false)

##### `admin dsl-debug session stop`
- Description: Stop a session (POST /dsl-debugging/session/{id}/stop)
- Usage: `swctl admin dsl-debug session stop [command options] <sessionId>`

Options:
- `--help, -h`: show help (default: false)

### `admin oal`
- Description: Browse the read-only OAL catalog (the OAL debugger's rule picker)
- Usage: `swctl admin oal command [command options] [arguments...]`
- Subcommands: `files`, `file`, `rules`, `rule`

Options:
- `--help, -h`: show help (default: false)

#### `admin oal files`
- Description: List the loaded .oal file names (GET /runtime/oal/files)
- Usage: `swctl admin oal files [command options]`

Options:
- `--help, -h`: show help (default: false)

#### `admin oal file`
- Description: Print the raw .oal text of one file (GET /runtime/oal/files/{name})
- Usage: `swctl admin oal file [command options] <name>`

Options:
- `--help, -h`: show help (default: false)

#### `admin oal rules`
- Description: List the per-dispatcher OAL source catalog (GET /runtime/oal/rules)
- Usage: `swctl admin oal rules [command options]`

Options:
- `--help, -h`: show help (default: false)

#### `admin oal rule`
- Description: Show one OAL source's per-metric holder status (GET /runtime/oal/rules/{source})
- Usage: `swctl admin oal rule [command options] <source>`

Options:
- `--help, -h`: show help (default: false)
